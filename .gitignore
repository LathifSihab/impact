# client-internal briefing documents — not for a public repo
brief/

# OS / editor noise
Thumbs.db
.DS_Store
.vscode/

# local screenshots / scratch output
_*.png
_*.txt

# The Astro app lives on feat/astro-cms until it reaches parity. Delete this line
# when that branch merges, or the merged files will be ignored on main.
/web/

# secrets — .env.example carries the names, never the values
.env
.env.*
!.env.example

# Python bytecode
__pycache__/
*.pyc
